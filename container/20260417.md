# 2026-04-17

## :family: Participants (GitHub handles preferred)
Participants (GitHub handles preferred)
- @jschoone
- @garloff
- @schneidarno

## 🧹 Housekeeping

## :notebook: Agenda
### Cluster Stacks Rancher Plugin (@schmidax)
- Repo needed (AI: @schmidax)
- Demo :star2:

### Release current set of cluster stacks (@garloff)
- CAPI-1.14 expected in August, won't support v1beta1
	- v1.35: Convert to v1beta2 first before release
	- v1.34: Already released with v1beta1, stick to it
		- Conversion of clusters should work, but clusterclasses are probably not converted
- Check if CSO will work with v1beta2 ClusterClasss (AI: @jschoone)
- AI @garloff: Create 1.33 and 1.34 test releases and test, @jschoone to review (and approve if good)

### Deal with breaking changes (@jschoone)
- [PR #255](https://github.com/SovereignCloudStack/cluster-stacks/pull/255), [PR #259](https://github.com/SovereignCloudStack/cluster-stacks/pull/259)
- ClusterStack name change prevented by CSO (Webhook)
	- Migration from scs->scs2 not smooth and scs3 won't be either (discussion in #255)
	- Move everything back to scs? (#259)
		- Create version subdirectories and deal with changes there
- Variables ...
	- overrides specifically for control plane or workers (machineDeployments)
	- has the advantage to avoid divergence between scs and hcp/kosmotron/kamaji
	- need to ensure we have SSD flavors by default for control plane in scs cluster classes, so they work

### Using registry.scs.community for node images? (@garloff)
- Uhurutec offered to build and push images
- registry (harbor) runs on gxscs2 (Plusserver test env)
- Need to decide -> 2026-05-xx

